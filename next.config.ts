import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['cdn-icons-png.flaticon.com'], // Add the external image domain here
  },
  // Add other config options as needed
};

export default nextConfig;
